2011-11-23
1、出现死循环，数据库表关联重复调用自身，应对其进行切断，例如：
jsonConfig.setExcludes(new String[]{"users", "rolesPrivilegeDetails"});
2、对状态进行了更改，单击更改状态
3、完成了右键菜单的增、删、改、
4、出现部署失败问题，更换了jdk1.7、配置文件中数据库名称不对应
--------------------------------------------------------------------------------------
2011-11-22
1、对右键菜单表添加了新的字段，编写了1对多筛选的问题，
2、看log，仔细分析，注意类型的转换
3、对右键菜单页面进行了完善
--------------------------------------------------------------------------------------
2011-11-21
1、解决了路径找不到错误，源文件存放位置文件名大小写问题，一定要注意导入包不能导错了
   例如：import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import org.springframework.web.servlet.ModelAndView;
import org.springframework.web.servlet.mvc.multiaction.MultiActionController;
2、注入问题
3、对右键菜单进行了设计，并完成了主页面、添加、删除、修改的基本功能，明天对其条件进行细化
--------------------------------------------------------------------------------------
2011-11-19
1、dispatcher-servlet配置中的，<bean id="test" class="ff.controller.TestController" />
 id必须小写
2、完成了右键菜单的初步设计
3、注意缺省包中的，配置文件applicationContext-dao，applicationContext-service中的注入问题id="小写"
--------------------------------------------------------------------------------------
2011-11-18
1、增加了一些新字段，更新了数据库，并且修改了，添加、修改、删除功能
2、对grid.Panel添加了鼠标右键菜单，
--------------------------------------------------------------------------------------
2011-11-17
1、仿照用户的删除，添加了删除功能、并对其进行了研究
2、对各个属性进行了排版
--------------------------------------------------------------------------------------
2011-11-16
1、对前台页面进行了设计，
2、做了添加、修改页面
--------------------------------------------------------------------------------------
2011-11-15
1、控制里面添加跟修改的时候Float、Integer类型进行转换Integer.valueOf(request.getParameter())
2、对其数据条件进行了设置
--------------------------------------------------------------------------------------
2011-11-14
1、仿照用户的设置对dao、dao.impl、service、service.implcontroller中的各个方法进行了编写
2、Spring方法注入一定要写set对其进行初始化
3、对程序的流程方法在dao、dao.impl、service、service.implcontroller中的调用
--------------------------------------------------------------------------------------
2011-11-12
1、对NetBeans运行java程序进行了流程的了解，并熟悉了其运用
2、熟悉了用户操作的一些功能
2、版本控制工具mercurial，进行了熟悉的使用，拉与推。合并冲突
--------------------------------------------------------------------------------------
2011-11-11
1、整理崂山党校的打印功能、传值问题，按批号打印、选择性打印
2、更改了页面的显示问题，增加了一个查询面板
3、运行防火平台、对照权限设置，增加了PTZ，方法调用的流程

```````````````````````````````````````````````````````````````````````````````````````````````````````
2011-11-10
  1、崂山党校，给图书管理模块添加编辑功能，
  2、添加一个新字段，状态
  3、更改了下载功能，添加一个上传按钮，先从本地上传excel表格，然后在下载---，以此来解决运行程序时可以对excel表格进行编辑
--------------------------------------------------------------------------------------
2011-11-09
  1、PTZ(云台的)的增、删、改、查，不要删除数据，变量名、字段名字标准
  2  model---dao---|dao.imple|---service---|service.imple|--controller----前台页面,流程
  3、编写完PTZ后台程序,仿照用户的操作，编写了基本的方法，添加、删除、按id查询某一条信息，得到所有的记录，
     controller中方法代码正在编写返回信息到页面的方法，正在编写前台显示、编辑、添加页面
  4、提交程序步骤---先从服务器刷下来(团队开发--共享--从缺省位置提取) --解决冲突(合并--合并更改，有冲突解决冲突)
     --本地mercurial提交--提交到服务器      
     
